# 🐍 Python 设计模式学习指南

> 配套练习文件：`design_patterns.py`（运行 `python design_patterns.py` 即可体验全部 12 种模式 + 7 道练习题）

---

## 一、为什么要学设计模式？

| 维度     | 没有设计模式           | 有了设计模式             |
| -------- | ---------------------- | ------------------------ |
| 代码复用 | 到处复制粘贴           | 一处定义，多处使用       |
| 扩展性   | 加功能要改核心代码     | 新增子类即可（开闭原则） |
| 可读性   | 新人看不懂为什么这么写 | "这是工厂模式"一句话说清 |
| 面试     | 被问懵                 | 信手拈来                 |

> 💡 **核心思想**：设计模式是前人对常见问题的**最佳实践总结**，不是死记硬背的教条。Python 由于动态语言的特性，很多模式的实现比 Java 简洁得多。

---

## 二、三大分类速览

| 分类          | 解决什么问题           | 包含模式                                   |
| ------------- | ---------------------- | ------------------------------------------ |
| 🏗️ **创建型** | 如何优雅地创建对象     | 单例、简单工厂、工厂方法、抽象工厂、建造者 |
| 🧱 **结构型** | 如何组织类与对象的关系 | 装饰器、适配器、代理、外观                 |
| 🧠 **行为型** | 如何管理对象之间的交互 | 策略、观察者、模板方法                     |

---

## 三、12 种模式详解

---

### 1. 单例模式 (Singleton)

> **「全局只有一个实例」**

**问题场景**：数据库连接池、模型缓存、全局配置——这些在整个程序中只需要一份。

**Python 四种实现对比**：

| 实现方式  | 难度   | 代码量 | 适用场景                       |
| --------- | ------ | ------ | ------------------------------ |
| 模块单例  | ⭐     | 0 行   | 配置文件、常量（**最推荐**）   |
| `__new__` | ⭐⭐   | 6 行   | 需要类语法的简单场景           |
| 装饰器    | ⭐⭐   | 8 行   | 想把任意类变成单例             |
| 元类      | ⭐⭐⭐ | 10 行  | 框架级别，控制整个类的创建过程 |

**Python 特色**：模块天然是单例的——`import config` 无论写多少次，返回的都是同一个模块对象。

**在 RAG 项目中**：`Embeddings.py` 中的模型加载——只需加载一次，后续 `embed()` 调用直接用缓存。

---

### 2. 简单工厂 (Simple Factory)

> **「一个工厂函数，根据参数返回不同的产品」**

**问题场景**：根据 `.env` 配置切换到不同的 LLM 提供商（OpenAI / Ollama）。

**核心代码骨架**：

```python
class Factory:
    @staticmethod
    def create(type: str):
        if type == "A": return ProductA()
        if type == "B": return ProductB()
        raise ValueError(f"Unknown: {type}")
```

**与工厂方法的区别**：

- 简单工厂：一个工厂类 + if-else 判断 → 新增产品要**修改**工厂代码
- 工厂方法：每个产品对应一个工厂子类 → 新增产品只需**新增**子类（开闭原则）

> 📌 实际项目中简单工厂足够用 80% 的场景，不要过度设计。

---

### 3. 工厂方法 (Factory Method)

> **「让子类决定创建哪个具体产品」**

**问题场景**：检索策略切换——纯向量检索 vs 关键词检索 vs 混合检索。

**核心思想**：

```
父类定义 create_search() → 抽象方法
子类重写 create_search() → 返回具体检索器
客户端只依赖父类，不关心具体实现（依赖倒置）
```

**与策略模式的区别**：

- 工厂方法：重点在**创建**（谁创建、什么时候创建）
- 策略模式：重点在**使用**（算法之间可以互相替换）

---

### 4. 抽象工厂 (Abstract Factory)

> **「创建一整套相关的产品族」**

**问题场景**：RAG Pipeline 需要 Embedding + LLM + Reranker 三件套，且要求三者兼容（不能混搭）。

**核心概念**：

```
MacFactory  → MacButton + MacTextBox   （苹果全家桶）
WinFactory  → WinButton + WinTextBox   （Windows 全家桶）
```

**使用时机**：只有当「一组产品必须一起使用」时才需要，否则工厂方法就够了。

---

### 5. 建造者模式 (Builder)

> **「分步构建复杂对象，最后调用 build()」**

**问题场景**：组装 RAG Prompt——系统指令 + 具体要求 + 参考材料 + 用户问题，每一步都可能省略。

**Python 特色链式调用**：

```python
prompt = (PromptBuilder()
    .set_system("你是助手")
    .add_context("材料1")
    .add_context("材料2")
    .set_question("什么是保研")
    .build())  # 触发验证
```

**核心价值**：

- 避免构造函数参数爆炸（`Prompt(a,b,c,d,e,f=None,g=None,...)`）
- 可以在 `build()` 时做校验
- 链式调用可读性极高

---

### 6. 装饰器模式 (Decorator)

> **「不修改原函数/类，动态添加功能」**

**这是 Python 最重要的设计模式之一**，因为 `@` 语法是语言级别的支持。

**三层嵌套原理**（带参数的装饰器）：

| 层次    | 接收什么                  | 返回什么    | 何时执行             |
| ------- | ------------------------- | ----------- | -------------------- |
| 第 1 层 | 装饰器参数 (如 `times=3`) | 第 2 层函数 | `@retry(times=3)` 时 |
| 第 2 层 | 被装饰函数 `func`         | 第 3 层函数 | Python 解释器加载时  |
| 第 3 层 | `*args, **kwargs`         | 实际返回值  | 每次调用 `func()` 时 |

**常用内置装饰器**：`@staticmethod`、`@classmethod`、`@property`、`@functools.lru_cache`

**实战场景**：

- `@timer` — 计时
- `@retry` — 重试
- `@cache` — 缓存
- `@log_calls` — 日志

---

### 7. 适配器模式 (Adapter)

> **「让不兼容的接口协同工作」**

**问题场景**：OpenAI 用 `client.chat.completions.create()`，Ollama 用 `requests.post()`，调用方式完全不同——业务代码不想关心这些细节。

**解决方案**：定义统一接口 `UnifiedLLM.chat(prompt)`，每个提供商各写一个适配器：

```
业务代码 → UnifiedLLM.chat()
              ├── OpenAIAdapter  → openai SDK
              └── OllamaAdapter  → requests.post()
```

**与外观模式的区别**：适配器解决**接口不兼容**，外观解决**系统太复杂**。

---

### 8. 代理模式 (Proxy)

> **「控制对对象的访问——限流、缓存、权限检查」**

**问题场景**：LLM API 有 QPS 限制，不能无限制调用。

**核心技巧**：代理和被代理对象实现**同一接口**，客户端感知不到差异。

```python
class RateLimitProxy(UnifiedLLM):
    def __init__(self, real_llm: UnifiedLLM, max_per_sec=5):
        self._llm = real_llm
        # 加限流逻辑

    def chat(self, prompt):
        self._check_rate_limit()
        return self._llm.chat(prompt)
```

**与装饰器的区别**：
| | 装饰器 | 代理 |
|------|--------|------|
| 粒度 | 函数级别 | 对象级别 |
| 写法 | `@decorator` | 包装另一个对象 |
| 控制能力 | 只能增强函数调用 | 可控制整个生命周期 |

---

### 9. 外观模式 (Facade)

> **「为复杂子系统提供简单入口」**

**问题场景**：RAG 问答流程是：检索 → 重排 → 拼 Prompt → 调 LLM → 返回。如果每次问答都手动调用这 4 步，代码会非常冗余。

**解决方案**：

```python
class RAGFacade:
    def ask(self, question: str) -> str:
        docs = self.retriever.search(question)    # 步骤1
        docs = self.reranker.rerank(question, docs)  # 步骤2
        prompt = self.builder.build(question, docs)  # 步骤3
        return self.llm.chat(prompt)                 # 步骤4

# 一行搞定！
answer = RAGFacade(...).ask("保研加分政策是什么")
```

---

### 10. 策略模式 (Strategy) ⭐ 最实用

> **「算法可互换——同一接口，不同实现」**

**问题场景**：相似度计算可以用余弦、欧氏距离、点积——运行时按需切换。

**核心结构**：

```
SimilarityStrategy (抽象)
    ├── CosineSimilarity
    ├── EuclideanDistance
    └── DotProduct

RAGEvaluator(strategy)    # 通过构造函数注入
    .set_strategy(...)     # 或通过 setter 切换
```

**与工厂方法的区别**：

- 工厂方法：关注**如何创建**对象
- 策略模式：关注**如何使用**算法（通常是组合+注入）

---

### 11. 观察者模式 (Observer)

> **「一对多的发布-订阅通知」**

**问题场景**：评测运行时，同时输出到控制台 + 写入 JSON 文件 + 记录到数据库。

**核心角色**：

```
EvalSubject（被观察者/主题）
    .attach(ConsoleLogger)
    .attach(JSONFileLogger)
    .attach(SQLiteLogger)
    .notify(event) → 所有 Logger 同时收到
```

**Python 中常被信号/回调替代**，但理解这个模式对阅读 Django Signals、Flask Signals 源码很有帮助。

---

### 12. 模板方法模式 (Template Method)

> **「固定流程骨架，步骤可替换」**

**问题场景**：评测流程固定是「加载→推理→打分→输出」，但不同的评测指标（准确率/MRR/NDCG）打分方式不同。

**核心思想**：

```
class EvalPipeline:
    def run(self):          # 模板方法（固定流程）
        加载数据()
        for item: 推理(item)  # 抽象方法（子类实现）
        打分()                # 抽象方法（子类实现）
        输出()
```

**与策略模式的区别**：

- 模板方法：流程固定，**步骤**可变（继承实现）
- 策略模式：**整个算法**可变（组合实现）

---

## 四、RAG 项目映射表

| 模式     | 你的项目文件      | 改造建议                                      |
| -------- | ----------------- | --------------------------------------------- |
| 单例     | `Embeddings.py`   | 模型加载用模块单例，避免重复加载              |
| 简单工厂 | `LLM.py`          | 根据 `.env` 中的 provider 创建不同 LLM 客户端 |
| 工厂方法 | `HybridSearch.py` | 向量/关键词/混合检索各一个工厂子类            |
| 抽象工厂 | 整体 Pipeline     | Embedding + LLM + Reranker 三件套不混搭       |
| 建造者   | `run_eval.py`     | 构建复杂评测 Prompt（系统指令+材料+问题）     |
| 装饰器   | `eval_logger.py`  | 给推理函数加 `@timer`、`@cache`、`@log`       |
| 适配器   | `LLM.py`          | 统一 OpenAI / Ollama 调用接口                 |
| 代理     | `LLM.py`          | 限流 + 缓存代理（API QPS 限制）               |
| 外观     | 新建 `RAGFacade`  | 一行 `ask()` 完成检索→重排→生成               |
| 策略     | `Reranker.py`     | 无重排 / 分数排序 / 规则排序可切换            |
| 观察者   | `eval_logger.py`  | 评测时多渠道日志输出                          |
| 模板方法 | `run_eval.py`     | 评测流程骨架（加载→推理→打分→输出）           |

---

## 五、SOLID 原则速记

| 字母  | 原则     | 一句话                 | 对应模式                 |
| ----- | -------- | ---------------------- | ------------------------ |
| **S** | 单一职责 | 一个类只干一件事       | 所有模式的基础           |
| **O** | 开闭原则 | 加功能不修改原有代码   | 工厂方法、策略、模板方法 |
| **L** | 里氏替换 | 子类能无缝替换父类     | 适配器、代理             |
| **I** | 接口隔离 | 不强迫依赖不需要的接口 | 把大接口拆小             |
| **D** | 依赖倒置 | 依赖抽象而非具体实现   | 外观、策略               |

> 🧠 口诀：**S-O-L-I-D 让代码更 SOLID（坚固）**

---

## 六、练习对照表

`design_patterns.py` 末尾的测试运行器会**自动验证**你的代码。7 道练习如下：

| 练习 | 模式     | 练习内容                                          | 核心考点                |
| ---- | -------- | ------------------------------------------------- | ----------------------- |
| 1    | 单例     | 补全 `singleton_exercise` 装饰器                  | 闭包 + `is` 判断        |
| 2    | 简单工厂 | 补全 `LLMFactory.create()`                        | if-else 分支 + 异常处理 |
| 3    | 工厂方法 | 补全三个 SearchFactory 子类                       | 抽象方法覆写            |
| 4    | 建造者   | 补全 `PromptBuilder`（链式调用 + `build()` 验证） | 返回 `self` + 断言      |
| 5    | 装饰器   | 补全 `@cache` 装饰器                              | 闭包 + 字典缓存         |
| 6    | 适配器   | 补全 `OllamaAdapter`                              | 实现抽象接口            |
| 7    | 策略     | 补全 `EuclideanDistance` 和 `DotProduct`          | 距离公式 → 相似度转换   |

---

## 七、推荐学习路线（6 天计划）

| 天数  | 内容                            | 运行命令                                  |
| ----- | ------------------------------- | ----------------------------------------- |
| Day 1 | 单例 4 种实现 + 练习 1          | `python design_patterns.py`（看前半输出） |
| Day 2 | 简单工厂 + 工厂方法 + 练习 2、3 | 逐段运行对应部分                          |
| Day 3 | 抽象工厂 + 建造者 + 练习 4      | 逐段运行对应部分                          |
| Day 4 | 装饰器 + 适配器 + 练习 5、6     | 重点理解三层嵌套                          |
| Day 5 | 代理 + 外观 + 策略 + 练习 7     | 对比装饰器 vs 代理                        |
| Day 6 | 观察者 + 模板方法               | 结合 `run_eval.py` 实战                   |

> ⚡ **快速复习**：只看本文档的第「三」章即可，12 个模式半小时过完。

---

## 八、推荐资源

| 资源                                                         | 类型     | 适合                     |
| ------------------------------------------------------------ | -------- | ------------------------ |
| [refactoring.guru](https://refactoring.guru/design-patterns) | 网站     | 图文并茂，有 Python 示例 |
| 《精通 Python 设计模式》                                     | 书籍     | 深入 Python 特性         |
| Django / Flask 源码                                          | 实践     | 看优秀项目怎么用设计模式 |
| `design_patterns.py`                                         | 你的练习 | 动手改代码，跑测试       |
